# Model-Counting-Experiments
Hardware design is hard

Usage: `make ToyExample.pmc.cnf`, then `approxmc ToyExample.pmc.cnf`.

Parameters are hard-coded in "Rtl/src/ToyExample.scala" and can be changed.
